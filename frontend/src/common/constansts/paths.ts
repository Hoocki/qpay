export enum Paths {
    Profile = "/profile",
    TopUp = "/topup",
    Transactions = "/transactions",
    Home = "/home",
    SignIn = "/signin",
    SignUp = "/signup"
}

export enum UserPaths {
    CUSTOMER = "/customers",
    MERCHANT = "/merchants",
    SIGN_UP_USER = "/signup"
}

export enum AuthPath{
    AUTH = "/auth",
    SIGN_IN_USER = '/signIn',
}