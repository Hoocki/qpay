export enum UserPaths {
    CUSTOMER = "/customers",
    MERCHANT = "/merchants",
    SIGN_UP_USER = "/signup"
}

export enum AuthPath{
    AUTH = "/auth",
    SIGN_IN_USER = '/signIn',
}

export enum WalletPaths {
    WALLET = "/wallets",
    WALLET_BY_USER = "/users"
}

export enum QRPath{
    QRCODE = "/qrcode",
}